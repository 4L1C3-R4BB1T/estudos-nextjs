import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Contato',
  description: 'Essa é a página contato',
};

export default function ContatoPage() {
  return (
    <main>
      <h1>Contato</h1>
    </main>
  );
}